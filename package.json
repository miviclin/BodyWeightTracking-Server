{
  "name": "bodyweight-tracking-api",
  "version": "0.0.1",
  "description": "Sample API that allows tracking bodyweight over time. This is a pet project just to try out swagger so it does not use a real database and it has not been created to be used as a real production server.",
  "main": "index.js",
  "scripts": {
    "prestart": "npm install",
    "start": "node index.js"
  },
  "keywords": [
    "swagger"
  ],
  "license": "MIT",
  "private": true,
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "lodash": "^4.17.4",
    "lokijs": "^1.5.0",
    "moment": "^2.18.1",
    "swagger-tools": "0.10.1"
  }
}
